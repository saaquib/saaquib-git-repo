#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#define MAX 32

extern int str_len(char *);

extern int is_valid(char s[]);

extern void is_mem_valid(char *);

void rem_ent(char *);

char* rotr_by_1(char *);

int is_rot_str(char *, char *);

char* sappend(char *, char *);

int str_spn(char *s1,char *s2);

void str_cpy(char *dbuf, char *sbuf);
