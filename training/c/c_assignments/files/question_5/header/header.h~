#include<stdio.h>
#include<string.h>
#include<stdlib.h>

struct EH {
	unsigned char e_ident[16];
	short e_type;
	short e_machine;
	int e_version;
	unsigned int e_entry;
};

int is_valid(char s[]);

void rem_ent(char *str);

int str_len(char *ch);
