#include"header.h"

void selection_sort(int *arr, int n)
{
	int i;
	int j;
	int min = 0;
	for ( i = 0; i < 5; i++) 
	{
		min = i;
		for (j = i + 1; j < 5; j++) 
		{
			if ( arr[j] < arr[min] ) 
				min = j;
		}
	swap(&arr[i], &arr[min]);
	}
}
